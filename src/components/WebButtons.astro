---
const { label, icon: Icon } = Astro.props;
---

<button class="flex items-center gap-2 px-4 py-2 border border-subtle text-muted text-sm rounded-full hover:bg-focus/10 transition-all duration-200">
  {Icon && <Icon class="text-fg" size={16}/>}
  <span>{label}</span>
</button>
